const path = require('path');
const multer = require('multer');

console.log(Date.now() + '-' + Math.round(Math.random() * 1E9) + '-blob');
console.log(Date.now() + '-' + Math.round(Math.random() * 1E9) + '-blob');
console.log(Date.now() + '-' + Math.round(Math.random() * 1E9) + '-blob');
